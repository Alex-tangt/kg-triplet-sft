# 仓库审计 — 2026-09-12

对象:`Alex-tangt/kg-triplet-sft`(Public,master,23 commits),工作树干净;本地 `pytest` 126 passed。
范围:主张-证据一致性 / 交付物缺口 / 文档漂移 / 公开仓卫生 / 可复现性 / 对简历的影响。
结论:**代码与实验纪律很强,但"对外可见性"和"文档-现实一致性"是短板。**

## P0 — 直接影响简历可验证性(优先)

1. **无根 README**。公开仓第一落点空白(无 30 秒概览、无关键数字、无复现入口)。
   证据:`git ls-files` 无 README;GitHub 仓库页无 README。
2. **证据不在仓库里**。`outputs/` 整体 gitignore,而所有硬数字只活在那里:
   base +0.34 表(`outputs/base_eval/RESULT.md`)、掩码修复链、容量曲线表、LightRAG T0/T1/T2
   ——面试官点开仓库**看不到任何量化证据**。这是"简历说得好、仓库查不到"的结构性风险。
3. **简历草稿被公开提交**:`docs/resume-project-column.md`(commit abc7afc),含占位符 `[A][B]`
   与内部证据指针,且已过时(仍写 MS GraphRAG)。个人求职件不应作为作品仓内容。
4. **无 LICENSE**:公开仓无授权默认"保留所有权利";vendored `eval/harness/` 无许可/来源声明
   (来自 `../HGR-finetuned-model-evaluation-pipeline`)。`kg_contract/graphrag_prompts.py` 用官方
   MS GraphRAG prompt(MIT)也应在 README 致谢。
5. **无数据集卡/来源与许可说明**(Wikipedia 60%/arXiv 40% + qwen 教师 + prompt 许可)。

## P1 — 文档与追踪器漂移(损害"诚实/严谨"人设)

6. **AGENTS.md Structure 与实况不符**:写 `serve/ Phase 4: export+GGUF+Ollama+Gradio`,实际
   `serve/` 是**空目录**、零提交;写 finetune 有 0.6/1.5/3B + 数据消融 configs + dataset card,
   实际只有 `qwen3_0.6b_graphrag_{full,tracer}.yaml`,无 dataset card,尺寸实为 1.7/4B,消融 deferred。
7. **`eval/README.md` 未同步 LightRAG**:作为"评测单一入口"却只写 GraphRAG 线,新的
   consumer-impact e2e(`eval/lightrag_*.py`)无 runbook。
8. **代码 docstring 残留 MS GraphRAG**:`dataset/graphrag.py:5`、`dataset/graphrag_pilot.py:7`、
   `eval/referent_reach.py:3`(仅 `student_prompt.py` 的"官方 MS GraphRAG prompt"表述是对的)。
9. **issue tracker 状态滞后**:03/06/07/10 实际已完成仍标 `ready-for-agent`;09 已 deferred;
   11/12/13 才是真 open。追踪器不能反映实况。
10. **Round-1 review gate 从未举行**;`spec.md` 验收线(0.6B composite 0.55–0.75、与 0.6583 同轴形)
    悬空且已与主线(referent/LightRAG)脱节。CONTEXT 自认 outstanding。

## P2 — 工程与可复现

11. **kernel 目录爆炸**:`finetune/kaggle/kernel_*` 20+ 个,大量 36–43KB 的 sed-clone 生成物
    (`infer/infer2/inferfull/inferprof/lfcheck/lffull/lftrain/cap06/cap06c/cap06m/cap06p/cap06ps/cap17/cap4b/capbase/bench/bench2/benchbatch…`),
    生成脚本与生成物同提交,可读性与维护性差,像"实验垃圾场"。
12. **依赖未锁**:根 `requirements.txt` 仅 5 行(pytest/datasets/numpy/mmh3/dotenv),无 torch/transformers/
    unsloth 版本;训练/推理环境无说明。只有 `requirements-lightrag.txt` 锁了 `lightrag-hku==1.5.7`。
13. **无 CI**(Actions 0);126 个测试在公开仓不自动验证。
14. **数据/版本可复现信息零散**:只有 run 目录的 `run_config.json` 有 data sha;无统一版本清单。

## P3 — 卫生

15. commit 邮箱公开(`2756363112@qq.com`)→ 个人可识别信息;可用 GitHub noreply。
16. HF adapter/merged 未发布(issue 13)→ 模型不可获取,复现止于代码。

## 做得好的(保留)

- ADR 纪律(0001–0009 + diary)强;commit 历史清晰可读;`.gitignore` 正确(无 outputs/data/.env 上仓);
  无硬编码密钥(仅 env 读取);测试通过;仓库描述与 Public 状态已就绪。

## 对简历的直接含义(必须先补)

- 简历若要放仓库链接,**README + curated evidence 表**是前提;否则链接反而暴露"无证据"。
- 简历草稿移出公开仓(或改为私有/非提交)。
- 所有"端到端有效"的措辞不能写;LightRAG 消费层是 null,只能写**可行性 + T0 结构传导**。
- 复刻线 composite/entity_f1 数字是原项目报告值,**本项目未复现**(Round-1 gate 未过),不得暗示已复现。
